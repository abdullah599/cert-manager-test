apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/abdullah599/cert-manager-test.git
    path: "."
    targetRevision: v1.15.3
    helm:
      releaseName: helix-cert-manager
      values: |
        installCRDs: true
  destination:
    server: https://kubernetes.default.svc
    namespace: helix-cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
